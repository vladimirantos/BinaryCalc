<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.tul.nti.antos.binarycalc.SettingsActivity">

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/enable_info_title"
        android:id="@+id/enableInfoPanel"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/format_text_spinner"
        android:entries="@array/format_arrays"
        android:layout_below="@+id/textView2"
        android:layout_alignParentStart="true"
        android:background="#ffdedede" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/format_display"
        android:id="@+id/textView2"
        android:textSize="18dp"
        android:gravity="center_vertical"
        android:textColor="#ff000000"
        android:layout_marginTop="80dp"
        android:layout_below="@+id/enableInfoPanel"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/save_button_title"
        android:id="@+id/button"
        android:layout_below="@+id/format_text_spinner"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="157dp" />

</RelativeLayout>
